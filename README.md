In order to run this program, you need to have python3 installed as well as pip. Please install scipy, matplotlib, and numpy using `pip install`, and then run `jupyter lab` in the base directory to get started.
